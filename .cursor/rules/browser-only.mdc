---
description: 配信は通常ブラウザの単ページWebのみ。ネイティブ殻は禁止。
alwaysApply: true
---

# ブラウザ専用（固定）

このリポジトリの成果物は **通常のブラウザで動く単ページWebアプリ** だけ。永続化は `localStorage`。静的ホスト（Vercel の静的出力など）で足りる。

禁止: Capacitor、Electron、React Native、Flutter、Tauri、Cordova、その他のネイティブ／モバイルアプリ殻。

v0 のゲーム仕様は変えない。Web SPA のまま続ける。
